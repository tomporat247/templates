version: 1

deploy:
  steps:
    onSuccess:
      - echo "key"
      - echo $ENV0_TOM_KEY
      - echo "reviewer id"
      - echo $ENV0_TOM_REVIEWER_id
      - echo "reviewer full"
      - echo "$ENV0_TOM_REVIEWER"
